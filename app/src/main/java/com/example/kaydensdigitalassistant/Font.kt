package com.example.kaydensdigitalassistant

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily


val font_archivo = FontFamily(
    Font(R.font.archivo)
)

val font_archivo_bold = FontFamily(
    Font(R.font.archivo_bold)
)

val font_archivo_light = FontFamily(
    Font(R.font.archivo_light)
)

val font_quicksand = FontFamily(
    Font(R.font.quicksand)
)

val font_notosans = FontFamily(
    Font(R.font.noto_sans)
)

val font_notosans_bold = FontFamily(
    Font(R.font.noto_sans_bold)
)

val font_notosans_light= FontFamily(
    Font(R.font.noto_sans_light)
)

val font_notosans_regular= FontFamily(
    Font(R.font.noto_sans_regular)
)

val font_abeezee = FontFamily(
    Font(R.font.abeezee)
)

val kanit_bold = FontFamily(
    Font(R.font.kanit_bold)
)
val kanit_semibold = FontFamily(
    Font(R.font.kanit_semibold)
)
val kanit_light = FontFamily(
    Font(R.font.kanit_light)
)
val kanit_mediym = FontFamily(
    Font(R.font.kanit_medium)
)
val kanit_regular = FontFamily(
    Font(R.font.kanit_regular)
)
